SELECT Title, ProductDescription FROM Book

SELECT * FROM Book

INSERT INTO Users (username, password, email)
VALUES (username, password, email);

Select * FROM Customers

Insert into InvoiceProductList (userID, bookID) 
VALUES (userID, bookID)

Insert into Book (Title, ProductDescription, State, Price)
Values (Title, ProductDescription, State, Price)

Update Book
Set Title=txtfieldtitle, ProductDescription=txtfieldproductDescription,
State=txtfieldSale, Price=txtfieldPrice
Where BookID = bookID

Delete FROM Book
Where BookID = bookID

Update Users
Set password=txtfieldpassword, email=txtfieldemail
Where userID = userID

Insert into Invoice (invoiceUsername, totalPrice)
Values(username, totalPrice)

Delete FROM InvoiceProductList
Where userID = userID


